import { makeStyles } from "@material-ui/core";
const useStyles = makeStyles((theme) => ({
  container: {
    width: "20%",
  },
  btn: {
    color: "#fff",
    textDecoration: "none",
  },
}));

export default useStyles;
